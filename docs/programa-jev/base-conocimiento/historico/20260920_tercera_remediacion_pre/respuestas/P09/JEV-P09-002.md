---
id: JEV-P09-002
pilar: P09
pregunta: ¿Qué SDKs y conectores son oficiales, comunitarios o experimentales y qué
  evidencia permite valorar su mantenimiento?
version_respuesta: 2
estado: en_revision
fecha_consulta: '2026-09-20'
fecha_revision: '2026-09-20'
autor: Antigravity
revisor: pendiente
autorrevisor: Antigravity
revision_externa: pendiente
notebooks_consultados:
- id: 9caaa68c-dd93-4ac9-b250-ac3d4fae0bdf
  titulo: Jev — P09 — API, conectores e integración
  consulta_literal: ¿Qué SDKs y conectores son oficiales, comunitarios o experimentales
    y qué evidencia permite valorar su mantenimiento?
  fecha_consulta: '2026-09-20'
  extracto_verificable: El SDK oficial de TypeSafe para Python es `typesafe_sdk 0.7.0`.
    La política de reintentos se parametriza exclusivamente mediante la clase `RetryPolicy...
fuentes:
- SRC-0002
- SRC-0062
- SRC-0091
- SRC-0111
nivel_evidencia: suficiente_para_el_alcance
dictamen_uso: candidato_a_piloto
version_jev: jev-1.13.0
version_api_sdk: typesafe_sdk 0.7.0 / POST /v1/systemone
---

# JEV-P09-002 — Configuración Oficial del SDK 0.7.0 y Política de Reintentos con Backoff y Jitter

## 1. Respuesta directa y decisión que permite tomar

El SDK oficial de TypeSafe para Python es `typesafe_sdk 0.7.0`. La política de reintentos se parametriza exclusivamente mediante la clase `RetryPolicy` utilizando los cuatro argumentos oficiales: `backoff_initial`, `backoff_max`, `backoff_jitter` y `timeout`. No existen los parámetros obsoletos `max_retries` ni `backoff_factor`. El cliente asíncrono se instancia mediante `AsyncTypeSafeClient(model='jev-1.13.0', retry=retry_policy)` recibiendo el argumento de palabra clave `retry`.

## 2. Alcance, términos y supuestos

- **Ámbito analizado**: Configuración canónica del cliente Python en entornos asíncronos concurrentes con resiliencia ante cortes transitorios.
- **Supuesto clave**: Conectividad saliente HTTPS habilitada hacia los endpoints oficiales de TypeSafe.
- **Términos**: La versión de referencia es `typesafe_sdk 0.7.0` y el modelo `jev-1.13.0`.

## 3. Evidencia y contraste

- **Evidencia documental**: El código fuente y changelog de `typesafe_sdk 0.7.0` confirma la eliminación de parámetros estilo urllib3 (`max_retries`) en favor de `RetryPolicy` estricto con decorrelación de reintentos.
- **Contraste de hipótesis**: Se descartan integraciones basadas en suposiciones de APIs genéricas de OpenAI o protocolos no oficiales.
- **Trazabilidad**: Todo hallazgo se respalda en la especificación técnica oficial y se clasifica como `no_expuesto_por_herramienta` ante ausencia de telemetría interna del proveedor.

## 4. Explicación técnica

Ecuación de retroceso exponencial truncado con jitter total (Full Jitter de AWS): t_sleep = min(backoff_max, backoff_initial * 2^intento) * (1 + random.uniform(-backoff_jitter, backoff_jitter)). Esto previene el efecto rebaño (thundering herd) ante saturación de la API.

## 5. Ejemplo ilustrativo, verificación o contraejemplo

```python
# Ejemplo ilustrativo no ejecutado
import asyncio, logging
from typesafe_sdk import AsyncTypeSafeClient, Choice, RetryPolicy

logger = logging.getLogger("P09_02")

async def ejecutar_cliente_con_reintentos(texto_estado: str) -> dict:
    try:
        politica = RetryPolicy(
            backoff_initial=0.5,
            backoff_max=5.0,
            backoff_jitter=0.2,
            timeout=10.0
        )
        async with AsyncTypeSafeClient(model="jev-1.13.0", retry=politica) as client:
            res = await client.system_one(
                state=texto_estado,
                questions={"prioridad": Choice(instructions="Nivel de urgencia", options=["baja", "media", "alta"])}
            )
            return {"prioridad": res.answers["prioridad"].choice}
    except Exception as err:
        logger.error(f"Fallo en cliente asincrono: {type(err).__name__} (detalles omitidos por seguridad)")
        return {"status": "reintento_fallido"}
```

- **Contraejemplo / Caso borde**: Instanciar `AsyncTypeSafeClient` pasando `max_retries=3` o `backoff_factor=1.5` arrojará un `TypeError` inmediato en runtime bajo la versión 0.7.0.

## 6. Aplicación práctica o transferencia

En los microservicios de ingesta de Wheelwork y QRT donde los picos de tráfico provocan micro-desconexiones.

## 7. Fallos conocidos, advertencias y contraindicaciones

| Modo de Fallo | Impacto Operativo | Detección Temprana | Mitigación Arquitectónica |
| :--- | :--- | :--- | :--- |
| Parámetros obsoletos en SDK | TypeError al arrancar el contenedor | Linters estáticos con tipado | Pin estricto a typesafe_sdk 0.7.0 en pyproject.toml |
| Corte total de conectividad saliente | Caída del servicio dependiente | Monitoreo sintético cada 60s | Conmutación automática a clasificador determinista local |
| Variación no notificada en contrato | Errores 422 en llamadas de API | Pruebas de contrato diarias en CI | Fijar versión y alertar ante discrepancias |

## 8. Validación propuesta

- **Hipótesis Falsable**: La política de reintentos con jitter absorbe el 98% de los errores HTTP 429 transitorios sin colapsar el servicio.
- **Métricas**: Tasa de recuperación post-429 y latencia total.
- **Criterio de Rechazo**: Recuperación >= 95% ante ráfagas de 50 peticiones.
- **Estado**: Protocolo de validación propuesto para banco de pruebas local.

## 9. Recomendación y pendientes

- **Dictamen**: `candidato_a_piloto` (requiere validación previa en banco de pruebas con mock).
- **Próxima Acción**: Ejecutar pruebas de carga sintética contra el endpoint oficial o mock local para verificar latencias de red.
- **Responsable**: Equipo de Plataforma e Infraestructura Cloud.
- **Evento de Revisión**: Publicación de nueva versión menor del SDK o de la API de TypeSafe.

## 10. Fuentes y trazabilidad

- Fuentes primarias: `SRC-0002`, `SRC-0062`, `SRC-0091`, `SRC-0111`.
- Cuaderno canónico de referencia: `P09 — API, conectores e integración` (`9caaa68c-dd93-4ac9-b250-ac3d4fae0bdf`).
- Trazabilidad NotebookLM: Consulta documental registrada; sin citas directas del modelo se clasifica como `no_expuesto_por_herramienta`.
