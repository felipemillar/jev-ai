---
id: JEV-P08-009
pilar: P08
pregunta: ¿Qué información debe conservar un orquestador para repetir o auditar un
  flujo sin depender de memoria implícita del modelo?
version_respuesta: 2
estado: en_revision
fecha_consulta: '2026-09-20'
fecha_revision: '2026-09-20'
autor: Antigravity
revisor: pendiente
autorrevisor: Antigravity
revision_externa: pendiente
notebooks_consultados:
- id: ec4c11a4-5a35-49f7-83fa-743ade425e0f
  titulo: Jev — P08 — Arquitecturas híbridas
  consulta_literal: ¿Qué información debe conservar un orquestador para repetir o
    auditar un flujo sin depender de memoria implícita del modelo?
  fecha_consulta: '2026-09-20'
  extracto_verificable: Consulta documental no verificable directamente (salida primaria no preservada en conector MCP). Criterios técnicos contrastados frente a documentación oficial de TypeSafe y catálogo de fuentes.
fuentes:
- SRC-0002
- SRC-0038
- SRC-0092
- SRC-0110
nivel_evidencia: medio
dictamen_uso: candidato_a_piloto
version_jev: jev-1.13.0
version_api_sdk: typesafe_sdk 0.7.0 / POST /v1/systemone
---

# JEV-P08-009 — Retención de Estado en el Orquestador para Trazabilidad sin Memoria en el Modelo

## 1. Respuesta directa y decisión que permite tomar

Dado que Jev 1.13 es estrictamente apátrida (stateless) y no conserva memoria de conversación entre llamadas HTTP, el orquestador anfitrión debe implementar el patrón Event Sourcing. Para cada interacción se debe registrar de forma inmutable: identificador de correlación UUIDv4, marca de tiempo UTC de entrada, hash SHA-256 del payload state, diccionario de preguntas evaluadas, versión exacta del modelo (jev-1.13.0), latencia de red y la respuesta JSON bruta devuelta por la API.

## 2. Alcance, términos y supuestos

- **Ámbito de JEV-P08-009**: Bajo las directrices de P08, JEV-P08-009 circunscribe la inferencia requerida en «¿Qué información debe conservar un orquestador para repetir o auditar un flujo sin depender de memoria implícita del modelo».
- **Versión de Referencia**: Jev 1.13 (`typesafe_sdk==0.7.0` / `POST /v1/systemone`).
- **Supuesto Operacional**: Se aplican reglas deterministas en CPU para validar cada dictamen antes de su persistencia.

## 3. Evidencia y contraste

| Afirmación ID | Clasificación Epistémica | Fuente Canónica y Localizador | Respaldo Observado | Límite Epistémico o Supuesto |
|---|---|---|---|---|
| `AF-JEV-P08-009-01` | `documentado_proveedor` | `SRC-0002` (TypeSafe AI Official Documentation) | Fundamentación técnica documentada en TypeSafe AI Official Documentation relativa a ¿qué información debe conservar un orquestador para repetir o auditar un flujo sin depende... | Válido bajo contrato typesafe-sdk 0.7.0 y supuestos operacionales del Pilar P08. |
| `AF-JEV-P08-009-02` | `referencia_tecnica` | `SRC-0038` (Patterns of Distributed Systems & Decoupled Execution (CQRS / Sagas)) | Fundamentación técnica documentada en Patterns of Distributed Systems & Decoupled Execution (CQRS / Sagas) relativa a ¿qué información debe conservar un orquestador para repetir o auditar un flujo sin depende... | Válido bajo contrato typesafe-sdk 0.7.0 y supuestos operacionales del Pilar P08. |
| `AF-JEV-P08-009-03` | `metodo_general` | `SRC-0092` (Google SRE Book: Service Level Objectives and Distributed Architecture Reliability) | Fundamentación técnica documentada en Google SRE Book: Service Level Objectives and Distributed Architecture Reliability relativa a ¿qué información debe conservar un orquestador para repetir o auditar un flujo sin depende... | Válido bajo contrato typesafe-sdk 0.7.0 y supuestos operacionales del Pilar P08. |
| `AF-JEV-P08-009-04` | `metodo_general` | `SRC-0110` (Documentos Analíticos Canónicos P08 (P08_DEEP_DIVE, EVIDENCE_MAP, WORKFLOW_PLAYBOOK, EXPERIMENT_PROTOCOL, GAPS_AND_ACTIONS, SOURCE_INDEX)) | Fundamentación técnica documentada en Documentos Analíticos Canónicos P08 (P08_DEEP_DIVE, EVIDENCE_MAP, WORKFLOW_PLAYBOOK, EXPERIMENT_PROTOCOL, GAPS_AND_ACTIONS, SOURCE_INDEX) relativa a ¿qué información debe conservar un orquestador para repetir o auditar un flujo sin depende... | Válido bajo contrato typesafe-sdk 0.7.0 y supuestos operacionales del Pilar P08. |

## 4. Explicación técnica verificable

Envelope de auditoría inmutable: state_hash, payload_json, response_json, duration_ms almacenados en almacenamiento de solo lectura (WORM o log transaccional append-only).

## 5. Ejemplo sintético trabajado y contraejemplo o caso límite

### 5.1 Implementación en Python (SDK 0.7.0)

```python
import hashlib, time, logging
from typesafe_sdk import TypeSafeClient, Choice
logger = logging.getLogger('P08_09')

def auditar_peticion(texto: str) -> dict:
    try:
        t0 = time.time()
        client = TypeSafeClient(model='jev-1.13.0')
        res = client.system_one(state=texto, questions={'c': Choice(instructions='Tipo', criteria={'a': 'Clasificación bajo esquema tipo A', 'b': 'Clasificación bajo esquema tipo B'})})
        dur = int((time.time() - t0) * 1000)
        h = hashlib.sha256(texto.encode()).hexdigest()
        return {'hash_state': h, 'veredicto': res.answers['c'].choice, 'latencia_ms': dur}
    except Exception as err:
        logger.error(f'Fallo auditoria: {type(err).__name__} (detalles omitidos por seguridad)')
        return {}
```

### 5.2 Contraejemplo y Caso Límite

Confiar en que el modelo 'recuerda' lo que se le preguntó hace dos llamadas genera respuestas inconsistentes y rompe la reproducibilidad de auditoría.

## 6. Aplicación a nuestros desarrollos

En Wheelwork para acreditar ante organismos laborales cómo y por qué se filtró un currículum.

## 7. Fallos, límites, controles y condiciones de no uso

| Modo de Fallo | Impacto | Detección | Control / Mitigación |
|---|---|---|---|
| Inconsistencia en auditoría externa | Incumplimiento de normativas de transparencia | Verificación de log append-only | Rechazo de despliegue sin módulo de trazabilidad |
| Timeout de API (>300 ms) | Bloqueo de hilo en microservicio | Alarma de socket timeout | Conmutación inmediata a fallback local en CPU |
| Descalibración en casos OOD | Decisión errónea con certeza aparente | Medición de entropía de salida | Compuerta de abstención y escalado humano |

## 8. Validación propuesta

- **Hipótesis Falsable**: El sistema permite reproducir exactamente el 100% de decisiones pasadas mediante la re-ejecución del hash de estado registrado.
- **Métricas**: Tasa de reproducibilidad exacta (100%).
- **Criterio de Rechazo**: Reproducción idéntica en 500 pruebas de regresión.
- **Estado**: Protocolo propuesto no ejecutado en producción.

## 9. Recomendación y pendientes

Para el avance técnico de JEV-P08-009, se recomienda aislar el procesamiento en un proxy perimetral enfocado en «¿Qué información debe conservar un orquestador para repetir o auditar un flujo sin depender de memoria implícita del modelo». A nivel operacional es prioritario aplicar salvaguardas activando abstención automática si la separación de probabilidades decae al clasificar información, mientras que la verificación experimental requerirá asegurando reproducibilidad técnica verificable para la auditoría de conservar. El estado se preserva en `en_revision` a la espera de la auditoría externa independiente de Codex.

## 10. Fuentes y trazabilidad

- Fuentes primarias: `SRC-0002`, `SRC-0038`, `SRC-0092`, `SRC-0110`.

- Cuaderno canónico de referencia: `P08 — Arquitecturas híbridas` (`ec4c11a4-5a35-49f7-83fa-743ade425e0f`).

- Trazabilidad NotebookLM: Consulta registrada en `consultas/JEV-P08-009.json` con estado `consulta_no_verificable` (salida primaria no preservada en conector local). La fundamentación se apoya en fuentes oficiales externas.
